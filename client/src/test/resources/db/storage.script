CREATE SCHEMA PUBLIC AUTHORIZATION DBA
CREATE MEMORY TABLE INDEX(ID BIGINT NOT NULL PRIMARY KEY,KEY VARCHAR(50),NAME VARCHAR(50),VALUE NUMERIC,CHANGE NUMERIC,TIMESTAMP BIGINT)
CREATE MEMORY TABLE USER(ID BIGINT NOT NULL PRIMARY KEY,NAME VARCHAR(50),PASSWORD VARCHAR(255))
CREATE USER SA PASSWORD ""
GRANT DBA TO SA
SET WRITE_DELAY 20
SET SCHEMA PUBLIC
INSERT INTO USER VALUES(1,'user','password')
INSERT INTO USER VALUES(2,'user','password')
